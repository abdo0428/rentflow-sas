<x-app-layout>
<x-slot name="breadcrumbs"><x-breadcrumbs :items="[__('app.units')=>route('units.index'), $unit->unit_number=>null]"/></x-slot>
<x-page-header :title="__('app.unit_number').' '.$unit->unit_number" :description="$unit->building?->name" icon="units"><x-slot name="actions"><x-detail-actions :record="$unit" module="units" :label="$unit->unit_number"/></x-slot></x-page-header>
<div class="grid items-start gap-6 xl:grid-cols-[minmax(0,2fr)_minmax(0,1fr)]"><x-card :title="__('property.unit_information')"><x-slot name="actions"><x-badge :value="$unit->status"/></x-slot><x-details-list :record="$unit" :fields="['unit_number','floor','type','bedrooms','bathrooms','area']"/></x-card><div class="space-y-6"><x-card :title="__('property.rent_availability')"><div class="p-6"><p class="muted">{{ __('app.rent_amount') }}</p><p class="mt-3 text-2xl text-slate-900"><x-data-value :value="$unit->rent_amount" column="rent_amount"/></p></div></x-card><x-card :title="__('app.building')"><div class="p-6"><p class="font-semibold text-slate-900">{{ $unit->building?->name }}</p><p class="muted mt-2">{{ $unit->building?->city }}</p>@can('view', $unit->building)<a href="{{ route('buildings.show',$unit->building) }}" class="text-link mt-4 inline-flex items-center gap-2 text-sm">{{ __('app.view') }}<x-icon name="arrow" class="h-4 w-4 rtl:rotate-180"/></a>@endcan</div></x-card></div></div>
@can('delete', $unit)<x-deletion-notice :reason="$deletionReason"/>@endcan
</x-app-layout>