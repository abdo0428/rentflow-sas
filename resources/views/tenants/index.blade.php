<x-app-layout>
<x-slot name="breadcrumbs"><x-breadcrumbs :items="[__('app.tenants') => null]"/></x-slot>
<x-page-header :title="__('app.tenants')" :description="__('property.tenants_intro')" icon="tenants"><x-slot name="actions">@can('create', \App\Models\Tenant::class)<a href="{{ route('tenants.create') }}" class="btn-primary"><x-icon name="plus" class="h-4 w-4"/>{{ __('property.add_tenant') }}</a>@endcan</x-slot></x-page-header>
<x-card>
<div class="flex flex-wrap items-center justify-between gap-4 border-b border-slate-100 p-5"><div><p class="text-sm font-medium text-slate-900">{{ __('property.total_tenants') }} <span class="ms-2 rounded-full bg-emerald-50 px-2.5 py-1 text-xs text-emerald-700">{{ $total }}</span></p></div><form action="{{ route('tenants.index') }}" method="GET" class="flex w-full flex-wrap items-end gap-2 sm:w-auto"><div class="min-w-0 flex-1"><x-field name="q" type="search" :label="'property.search'" :value="$filters['q'] ?? ''" :placeholder="__('property.search_tenants')" :required="false"/></div><button class="btn-primary">{{ __('property.search') }}</button><a href="{{ route('tenants.index') }}" class="btn-secondary" aria-label="{{ __('property.clear_filters') }}"><x-icon name="close" class="h-4 w-4"/></a></form></div>
@if($tenants->isEmpty())<x-empty-state :title="$total ? __('property.no_results') : __('property.empty_tenants')" :description="$total ? __('property.no_results_hint') : __('property.empty_tenants_hint')" icon="tenants"/>
@else<x-data-table :caption="__('app.tenants')" :headings="[__('app.full_name'), __('app.email'), __('app.phone'), __('property.lease_count'), __('app.actions')]">@foreach($tenants as $tenant)<tr><td><a href="{{ route('tenants.show', $tenant) }}" class="flex items-center gap-3"><span class="flex h-10 w-10 items-center justify-center rounded-full bg-emerald-50 font-semibold text-emerald-700">{{ mb_substr($tenant->full_name, 0, 1) }}</span><span class="font-semibold text-slate-900">{{ $tenant->full_name }}</span></a></td><td><span dir="ltr">{{ $tenant->email }}</span></td><td><span dir="ltr">{{ $tenant->phone }}</span></td><td><span class="rounded-md bg-slate-100 px-2.5 py-1 text-xs font-medium">{{ $tenant->leases_count }}</span></td><td><x-record-actions :record="$tenant" module="tenants" :label="$tenant->full_name"/></td></tr>@endforeach</x-data-table><div class="p-5">{{ $tenants->links('components.pagination') }}</div>@endif
</x-card></x-app-layout>