@props(['name', 'value' => '', 'label' => null, 'required' => false])
<div><label class="label" for="{{ $name }}">{{ $label ?? __('property.attributes.'.$name) }} @unless($required)<span class="ms-1 font-normal text-slate-400">({{ __('property.optional') }})</span>@endunless</label><textarea id="{{ $name }}" name="{{ $name }}" @required($required) rows="4" aria-invalid="{{ $errors->has($name) ? 'true' : 'false' }}" aria-describedby="{{ $name }}-error" {{ $attributes->class(['field resize-y']) }}>{{ old($name, $value) }}</textarea><x-input-error :messages="$errors->get($name)" id="{{ $name }}-error" class="mt-2"/></div>