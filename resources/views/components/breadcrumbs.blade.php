@props(['items' => []])
<nav aria-label="{{ __('property.breadcrumbs') }}" class="min-w-0 overflow-hidden"><ol class="flex items-center gap-2 text-sm"><li class="hidden shrink-0 sm:block"><a href="{{ route('dashboard') }}" class="rounded text-slate-400 hover:text-emerald-700" aria-label="{{ __('app.dashboard') }}"><x-icon name="dashboard" class="h-4 w-4"/></a></li>@foreach($items as $label => $url)<li @class(['flex min-w-0 items-center gap-2', 'hidden sm:flex' => ! $loop->last])><x-icon name="arrow" class="hidden h-3 w-3 shrink-0 text-slate-300 rtl:rotate-180 sm:block"/>@if($url)<a href="{{ $url }}" class="truncate text-slate-500 hover:text-emerald-700">{{ $label }}</a>@else<span aria-current="page" class="truncate font-medium text-slate-800">{{ $label }}</span>@endif</li>@endforeach</ol></nav>