<x-app-layout><x-slot name="header">{{ __('app.settings') }}</x-slot><div><h1 class="page-title">{{ __('app.settings') }}</h1><p class="muted mt-2">{{ __('app.settings_intro') }}</p></div><div class="card max-w-3xl space-y-5 p-6"><h2 class="font-semibold text-slate-900">{{ $company?->name ?? __('app.system_workspace') }}</h2>@if($company)<dl class="grid gap-5 sm:grid-cols-2">@foreach(['email','phone','address','status','trial_ends_at'] as $column)<div><dt class="muted">{{ __('app.'.$column) }}</dt><dd class="mt-1 text-sm"><x-data-value :value="$company->$column" :column="$column"/></dd></div>@endforeach</dl>@endif<div class="flex flex-wrap items-center justify-between gap-4 border-t border-slate-100 pt-5"><span class="text-sm">{{ __('app.language') }}</span><x-language-switcher/></div><a href="{{ route('profile.edit') }}" class="btn-secondary"><x-icon name="profile"/>{{ __('app.profile') }}</a></div></x-app-layout>